"use client";

import { useMemo, useState } from "react";
import Link from "next/link";
import { ArrowUpRight, CheckCircle2, ClipboardList, RefreshCw, Search, Users } from "lucide-react";
import { useGetAdminRequestsQuery } from "@/features/admin/adminRequestsApi";
import type { AdminRequestItem } from "@/features/admin/adminRequestsApi";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator } from "@/components/ui/breadcrumb";
import { Empty, EmptyContent, EmptyDescription, EmptyHeader, EmptyMedia, EmptyTitle } from "@/components/ui/empty";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Skeleton } from "@/components/ui/skeleton";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { REQUEST_STATUS_AR, RequestStatus } from "@hassad/shared";
import { formatDateTime, formatNumber } from "@/lib/format";

function statusVariant(status: string) { switch (status) { case RequestStatus.SIGNED: case RequestStatus.PROJECT_CREATED: return "secondary"; case RequestStatus.CANCELLED: return "destructive"; default: return "outline"; } }

function LoadingState(){return <div dir="rtl" className="flex flex-col gap-6 p-4 sm:p-6 lg:p-8"><Card><CardHeader className="gap-4 lg:flex-row lg:items-end lg:justify-between"><div className="flex flex-col gap-2"><Skeleton className="h-4 w-24"/><Skeleton className="h-9 w-56"/><Skeleton className="h-4 w-full max-w-xl"/></div><div className="flex gap-2"><Skeleton className="h-10 w-28"/><Skeleton className="h-10 w-32"/></div></CardHeader></Card><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{Array.from({ length: 4 }).map((_, i)=><Card key={i}><CardContent className="flex items-start justify-between gap-4 p-6"><div className="flex flex-col gap-2"><Skeleton className="h-4 w-24"/><Skeleton className="h-8 w-16"/><Skeleton className="h-4 w-32"/></div><Skeleton className="size-10 rounded-lg"/></CardContent></Card>)}</div><Card><CardHeader className="gap-2"><Skeleton className="h-6 w-36"/><Skeleton className="h-4 w-72"/></CardHeader><CardContent className="flex flex-col gap-4"><div className="grid gap-3 lg:grid-cols-[minmax(0,1fr)_220px] max-w-3xl"><Skeleton className="h-10 w-full"/><Skeleton className="h-10 w-full"/></div><div className="overflow-hidden rounded-lg border"><Table><TableHeader><TableRow>{Array.from({ length: 8 }).map((_, index)=><TableHead key={index}><Skeleton className="h-4 w-full"/></TableHead>)}</TableRow></TableHeader><TableBody>{Array.from({ length: 6 }).map((_, row)=><TableRow key={row}>{Array.from({ length: 8 }).map((_, cell)=><TableCell key={cell}><Skeleton className="h-5 w-full"/></TableCell>)}</TableRow>)}</TableBody></Table></div></CardContent></Card></div>}

export default function RequestsPage(){const [search,setSearch]=useState("");const [status,setStatus]=useState<"ALL"|RequestStatus>("ALL");const {data,isLoading,isError,isFetching,refetch}=useGetAdminRequestsQuery({limit:1000,search:search||undefined,status:status==="ALL"?undefined:status});const requests=data?.items??[];const metrics=useMemo(()=>({total:requests.length, signed:requests.filter((i)=>i.status===RequestStatus.SIGNED).length, projectCreated:requests.filter((i)=>i.status===RequestStatus.PROJECT_CREATED).length, waiting:requests.filter((i)=>i.status===RequestStatus.SUBMITTED||i.status===RequestStatus.QUALIFYING).length, services:requests.reduce((s,i)=>s+(i.servicesCount||0),0)}),[requests]);if(isLoading)return <LoadingState/>;if(isError)return <div dir="rtl" className="p-4 sm:p-6 lg:p-8"><Card><CardContent className="p-8"><Empty><EmptyMedia variant="icon"><ClipboardList/></EmptyMedia><EmptyHeader><EmptyTitle>تعذر تحميل الطلبات</EmptyTitle><EmptyDescription>حدث خطأ أثناء جلب بيانات الطلبات. حاول مرة أخرى.</EmptyDescription></EmptyHeader><EmptyContent><Button onClick={()=>refetch()}>إعادة المحاولة</Button></EmptyContent></Empty></CardContent></Card></div>;return <div dir="rtl" className="flex flex-col gap-6 p-4 sm:p-6 lg:p-8"><Card><CardHeader className="gap-4 lg:flex-row lg:items-end lg:justify-between"><div className="flex flex-col gap-3"><Breadcrumb><BreadcrumbList><BreadcrumbItem><BreadcrumbLink asChild><Link href="/dashboard">الرئيسية</Link></BreadcrumbLink></BreadcrumbItem><BreadcrumbSeparator/><BreadcrumbItem><BreadcrumbPage>الطلبات</BreadcrumbPage></BreadcrumbItem></BreadcrumbList></Breadcrumb><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-lg bg-primary/10 text-primary"><ClipboardList/></div><div className="flex flex-col gap-1"><CardTitle className="text-2xl">قائمة الطلبات</CardTitle><CardDescription>متابعة الطلبات قبل التحويل إلى عرض سعر أو عقد.</CardDescription></div></div></div><div className="flex flex-wrap gap-2"><Button variant="outline" size="sm" onClick={()=>refetch()}><RefreshCw/>{isFetching?"جاري التحديث":"تحديث"}</Button></div></CardHeader></Card><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[{label:"إجمالي الطلبات",value:formatNumber(metrics.total),hint:"كل الطلبات",icon:ClipboardList},{label:"بانتظار المعالجة",value:formatNumber(metrics.waiting),hint:"طلبات جديدة/تأهيل",icon:Users},{label:"تم توقيعها",value:formatNumber(metrics.signed),hint:"طلبات تحولت إلى عقد",icon:CheckCircle2},{label:"عدد الخدمات",value:formatNumber(metrics.services),hint:"إجمالي الخدمات المضافة",icon:ClipboardList}].map((item)=><Card key={item.label}><CardContent className="flex items-start justify-between gap-4 p-6"><div className="flex flex-col gap-2"><span className="text-sm text-muted-foreground">{item.label}</span><span className="text-2xl font-semibold tracking-tight">{item.value}</span><span className="text-sm text-muted-foreground">{item.hint}</span></div><div className="flex size-10 items-center justify-center rounded-lg bg-muted text-muted-foreground"><item.icon/></div></CardContent></Card>)}</div><Card><CardHeader className="gap-2"><CardTitle>سجل الطلبات</CardTitle><CardDescription>اضغط على السطر لفتح تفاصيل الطلب.</CardDescription></CardHeader><CardContent className="flex flex-col gap-4"><div className="grid gap-3 lg:grid-cols-[minmax(0,1fr)_220px] max-w-3xl"><div className="relative"><Search className="pointer-events-none absolute top-1/2 right-3 -translate-y-1/2 text-muted-foreground"/><Input value={search} onChange={(e)=>setSearch(e.target.value)} placeholder="ابحث باسم الشركة أو جهة الاتصال" className="pr-10"/></div><Select value={status} onValueChange={(v)=>setStatus(v as "ALL"|RequestStatus)}><SelectTrigger><SelectValue placeholder="كل الحالات"/></SelectTrigger><SelectContent><SelectItem value="ALL">كل الحالات</SelectItem>{(Object.values(RequestStatus) as RequestStatus[]).map((value)=><SelectItem key={value} value={value}>{REQUEST_STATUS_AR[value]}</SelectItem>)}</SelectContent></Select></div><div className="overflow-hidden rounded-lg border"><Table><TableHeader><TableRow><TableHead>الشركة</TableHead><TableHead>جهة الاتصال</TableHead><TableHead>الحالة</TableHead><TableHead>الخدمات</TableHead><TableHead>العمر</TableHead><TableHead>آخر تحديث</TableHead><TableHead className="text-left">التفاصيل</TableHead></TableRow></TableHeader><TableBody>{requests.length===0?<TableRow><TableCell colSpan={7} className="text-center text-muted-foreground">لا توجد نتائج</TableCell></TableRow>:requests.map((request:AdminRequestItem)=><TableRow key={request.id}><TableCell><Link href={`/dashboard/admin/requests/${request.id}`} className="font-medium transition-colors hover:text-primary">{request.clientName}</Link><div className="text-xs text-muted-foreground">{request.id}</div></TableCell><TableCell>{request.assigneeName || "—"}</TableCell><TableCell><Badge variant={statusVariant(request.status)}>{REQUEST_STATUS_AR[request.status as RequestStatus] || request.status}</Badge></TableCell><TableCell>{formatNumber(request.servicesCount)}</TableCell><TableCell>{formatNumber(request.ageDays)} يوم</TableCell><TableCell>{formatDateTime(request.createdAt)}</TableCell><TableCell className="text-left"><Button variant="ghost" size="sm" asChild><Link href={`/dashboard/admin/requests/${request.id}`}><ArrowUpRight/>فتح</Link></Button></TableCell></TableRow>)}</TableBody></Table></div></CardContent></Card></div>;}
