# Sales Dashboard → Design-System Migration

## Progress (Sunday, Jun 07 2026)

### Completed ✅

1. **Phase 1 — Rename + Import Fix**
   - `components/portal/` → `components/design-system/` with clean exports.
   - All 119+ imports across codebase updated (**7 files** fixed to avoid import-name clashes).
   - Build passes.
2. **Phase 2 — Tables**
   - `ContractsTable.tsx`/`ProposalsTable.tsx` (sales) — migrated to `DataTable` + `ActionButton`, removed old imports.
   - `ClientsTable.tsx` (crm) — migrated to `DataTable` + `Pill` (with `STATUS_TONE` mapping), removed old imports.
   - `ClientsTableSkeleton.tsx` — migrated to `Skeleton`.
   - `ClientFiltersBar.tsx` — migrated to `ActionButton`.
3. **Phase 3 — Request Detail**
   - `requests/[id]/page.tsx` — migrated `Card` → `SurfaceCard`, `Badge` → `Pill` (with `STATUS_TONE`), `Skeleton` → `Skeleton`, error → `StatusBanner`.
4. **Phase 4 — Forms & CRM Dialogs**
   - `requests/new/page.tsx` — migrated `Button` → `ActionButton`, kept raw Form/Input/Select/Checkbox (no wrappers exist).
   - `CreateClientDialog.tsx` (crm) — migrated to DS `Dialog` + `ActionButton`; kept raw Form/Select/Input.
   - `ContactAttemptDialog.tsx` (crm) — migrated to DS `Dialog` + `ActionButton`.
5. **Phase 5 — Kanban Re-skin**
   - `KanbanGroup.tsx` — migrated `Badge` → `Pill`. Board/Column/Card keep color-coding as instructed.
6. **Phase 6 — Client Info & Timeline**
   - `ClientInfoCard.tsx` — `Card` → `SurfaceCard`, `Badge` → `Pill`, `Button` → `ActionButton`.
   - `ClientTimeline.tsx` — `Card` → `SurfaceCard`.
   - `RequirementsForm.tsx` — `Card` → `SurfaceCard`, added `Pill` tone mapping for status.
   - `HandoverModal.tsx` (dialog) — migrated header → DS `Dialog` + `ActionButton`, kept raw Form/Input/SearchCombobox.
   - `sales/clients/[id]/page.tsx` — migrated `Skeleton` → `Skeleton`, `Button` → `ActionButton`.
   - `admin/clients/[id]/page.tsx` — migrated `Skeleton` → `Skeleton`, `Button` → `ActionButton`, `Card` → `SurfaceCard` for project/invoice/chat/contract cards.
7. **Phase 7 — Heavy Dialogs (IN PROGRESS)**
   - `CreateContractDialog.tsx` — imports restored from `git checkout` after a botched `edit` caused unmatched brackets. Reverted to original + cleaned imports (design-system `Skeleton` + `ActionButton` + kept raw `Dialog`/`Form`/`Select`/`Input`). **Build passes pending.**
   - `ProposalFormDialog.tsx` — only has raw `Dialog` import; will handle next.

### Remaining (targeted for next session) 📋

- `CreateContractDialog.tsx` sequential `Button` → `ActionButton` swap (trigger + submit actions).
- `ProposalFormDialog.tsx` swap `Dialog` import + any `Button` usage.
- `IntakeForm.tsx`, `IntakeFormModal.tsx` CRM files.
- `LeadsDialog.tsx` if exists (imported in leads pages).
- Marketing/PM/Employee/Admin check (grep shows raw imports there too but those are outside sales scope).

## Notes

- **Design system exemptions applied**: `SearchCombobox` remains raw (no wrapper), `Collapsible` stays raw.
- `ActionButton` does **not** support `title` prop → removed from `ProposalsTable.tsx` edit button.
- `DataTable` structural cells correctly imported from `@/components/ui/table`.
- **Current limitation**: DS `Dialog` lacks `DialogTrigger` slot — all dialog triggers rendered outside as separate `ActionButton`.
- `CreateContractDialog.tsx` is ~700 lines with two complex form branches; migration must be bracket-safe.
