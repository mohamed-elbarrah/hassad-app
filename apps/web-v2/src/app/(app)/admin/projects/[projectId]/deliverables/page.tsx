import type { Metadata } from "next";

import { ScreenPlaceholder } from "@/components/patterns/screen-placeholder";

export const metadata: Metadata = {
  title: "Project Deliverables & Revisions | Hassad",
};

export default function ProjectDeliverablesPage() {
  return <ScreenPlaceholder label="Project Deliverables & Revisions" />;
}
