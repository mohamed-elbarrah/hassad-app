import type { Metadata } from "next";

import { ScreenPlaceholder } from "@/components/patterns/screen-placeholder";

export const metadata: Metadata = {
  title: "Project Period Detail | Hassad",
};

export default function ProjectPeriodDetailPage() {
  return <ScreenPlaceholder label="Project Period Detail" />;
}
